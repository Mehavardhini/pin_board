%h2= @pin.title
= image_tag @pin.image.url, class: "image_pin"
%p= @pin.description
%p
Submitted by
= @pin.user.email
%br/

= link_to "Back", pins_path
= link_to "Edit", edit_pin_path(@pin)
= link_to "Delete", pin_path, method: :delete, data: { confirm: "Do you really want to delete this pin?" }
